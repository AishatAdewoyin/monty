#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @lineCount: lineCount
 * Return: no return
*/
void s_stack(stack_t **head, unsigned int lineCount)
{
	(void)head;
	(void)lineCount;
	bus.lifi = 0;
}
