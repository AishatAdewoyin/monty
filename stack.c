#include "monty.h"
/**
 * s_stack - prints the top
 * @head: head pointer
 * @lineNum: line counter
 * Return: nothing
*/
void s_stack(stack_t **head, unsigned int lineNum)
{
	(void)head;
	(void)lineNum;
	bus.lifi = 0;
}
