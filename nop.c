#include "monty.h"
/**
  *s_addnop- do nothing
  *@head: pointer to stack head
  *@lineCount: lineCount
  *Return: void
 */
void s_nop(stack_t **head, unsigned int lineCount)
{
	(void) lineCount;
	(void) head;
}
