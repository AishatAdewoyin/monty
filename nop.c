#include "monty.h"
/**
  *s_nop- do nothing
  *@head: head pointer
  *@lineNum: line counter
  *Return: nothing
 */
void s_nop(stack_t **head, unsigned int lineNum)
{
	(void) lineNum;
	(void) head;
}
